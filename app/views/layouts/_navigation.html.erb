<div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      
      <%= link_to image_tag("simpleweed_h_w.png", height: '53', width: '150'), root_path, 'data-no-turbolink' => true, class: "navbar-brand simple-weed-logo" %>          
          
      </a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
      </ul>
      <!-- <form class="navbar-form navbar-left" role="search"> -->
      <% if !@disableSearchInNav %>
      
      <!-- item search -->
      <%= form_tag(search_path, :method => "get", class: "navbar-form navbar-left", id: "item_search-form") do %>
        <div class="form-group">          
          <%= text_field_tag :itemsearch, params[:itemsearch], class: "form-control search-inputs", placeholder: "search items, dispensaries" %>
          <%= text_field_tag :itemsearch_location, params[:itemsearch_location], class: "form-control search-inputs", placeholder: "location" %>          
        </div>
        <button type="submit" id="submit-item-search" class="btn btn-default"><span class="glyphicon glyphicon-search"></span></button>
        
        <button id="show-search-filters" class="btn "><span class="glyphicon glyphicon-filter"></span></button>
        
        <button type="submit" id="group-search-results" class="btn "><span class="glyphicon glyphicon-list"></span></button>
        <script>
          $("#group-search-results").click(function(){
            var input = $("<input>")
               .attr("type", "hidden")
               .attr("name", "groupbystore").val("true");
              $('#item_search-form').append($(input));
              })

        </script>
          <% end %>
        
      </form>


      <% end %>
      <ul class="nav navbar-nav navbar-right">        
          <% if user_signed_in? %>            
            <li>
              <%= link_to current_user.username, edit_user_registration_path, :id => 'edit-user-link',:class => 'navbar-link' %>
            </li>
            <li>
              <%= link_to "log out", destroy_user_session_path, :id => 'logout-link', method: :delete, :class => 'navbar-link'  %>
            </li>
          <% else %>
          <li>
            <%= link_to 'sign up', registration_modal_path , data: { toggle: "modal", target: "#modal", remote: registration_modal_path} , id: "register-link" %>            
          </li>
          <li>             
            <%= link_to 'login', login_path , data: { toggle: "modal", target: "#modal", remote: login_path} , id: "log-in-link" %>
          </li>
          <% end %>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</div>