
(function(response) {
	//27_vote_sum
	id = response.review_id
	$("#" + id + "_vote_sum").html(response.currentcount)	
})(
	{
		message:'ok',	
		review_id: <%= @store_item_review.id %>,
		vote: <%= @vote %>,
		currentcount: <%= @currentcount %>
	}
)